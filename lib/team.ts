/**
 * Real team member photos and names. Empty until Noah supplies them — never
 * invent a name or use a stand-in photo. The About page team section
 * renders only when this array is non-empty.
 */
export type TeamMember = { name: string; role: string; photoSrc: string; photoAlt: string };

export const TEAM: TeamMember[] = [];
